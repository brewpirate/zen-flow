---
name: compare
description: Run cross-model comparison of trigger phrase generation (haiku vs sonnet vs opus)
arguments: path - File or directory to compare
user_invocable: true
---

Load and follow the `total-recall:compare` skill.

Pass user arguments as context — they specify which file or directory to compare.
